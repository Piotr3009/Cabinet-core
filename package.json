{
  "name": "cabinet-core",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Parametric cabinet & fitted-furniture configurator for joinery workshops",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test \"test/**/*.test.js\""
  },
  "comment:versions": "Versions pinned EXACTLY to reference/production-core/package.json (CLAUDE.md rule 5). Sole exception: @vitejs/plugin-react keeps PC's caret range \u2014 4.3.3 declares peer vite ^4||^5 and cannot install next to the pinned vite 7.1.3, so the range resolves to the 4.x that supports vite 7 (this is how it resolves in PC too). Libraries PC has but Cabinet Core does not need are omitted: react-router-dom, xlsx, pdf-lib, jspdf-autotable.",
  "dependencies": {
    "@react-three/drei": "10.7.6",
    "@react-three/fiber": "9.4.0",
    "@supabase/supabase-js": "2.45.4",
    "jspdf": "2.5.2",
    "react": "19.1.1",
    "react-dom": "19.1.1",
    "three": "0.180.0",
    "zustand": "5.0.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.3",
    "autoprefixer": "10.4.20",
    "postcss": "8.4.47",
    "tailwindcss": "3.4.14",
    "vite": "7.1.3"
  }
}
