# Cabinet Core — optional Supabase connection.
# Copy to `.env` and fill in to enable cloud projects + auth.
# WITHOUT this file the app still runs, in "Mock data mode" (yellow badge).
#
# Supabase dashboard -> Project Settings -> API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
