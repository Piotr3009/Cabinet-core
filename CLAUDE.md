# CLAUDE.md — TURN 70 · THE SHOE BOX MOVES AS ONE, AND FRONTS GET THEIR OWN VOICE

Run autonomously. Zero questions, zero stops. Skip-and-note. PR before morning.
Full suite, never `--silent`. Frames under `verify/t70/`. The session never
halts: "stop" means "stop that one feature", never the night.

## LAWS
**1:1 = COPY** (copies stay copies; hide via `RETAIL_SHOW_WORKSHOP_TOOLS`,
never delete). **Left adds, right edits.** **Material never enters material.**
**Vertical before horizontal.** **Diagnose before you cut** — F6 begins with a
committed probe. **LISP IS LAW** — F1 touches cut geometry, so the kit states
it before the engine does.

## TWO DECISIONS MADE HERE
The owner ordered the spec with two questions open. Decided, overturnable with
one word:
1. **The shoe box's sloping shelf is PART of the box** — one moving assembly,
   one set of runners, it comes out with the box. *(Overturn: "skos osobno".)*
2. **"2–3 typy kolorów frontów" = more than one front colour in a design** —
   the client may give a second and a third colour to chosen fronts. The store
   already holds `setUnitFinish`/`resetUnitFinish`, unused since T60's parity
   map. *(Overturn with what you meant.)*

## FROZEN
1. PRO: T67's EXEMPT map does not grow. F1 is ENGINE work, so PRO inherits it
   with no component edit — that is why it lands in both apps for free.
   If a PRO component edit seems needed, skip and name it.
2. Goldens ×6 byte-identical. **F1 is cut geometry**: if a fixture carries a
   shoe box, the change WILL move it — then STOP that feature, name the
   fixture, and go on. Never re-bless.
3. Engine licence: `cabinet.js` (F1 only, the shoe box's own path),
   `profile.js` (F3 keys, F5 sheen). `doors.js`, `room.js` read-only.
4. Parens 14/14 at 0/0 after the kit edit; classifier names every delta.

## F1 · THE SHOE BOX IS ONE MOVING THING
Owner: *"szuflada na buty super, że traktujesz jak normalną szufladę, ale nie
może mieć półki nad sobą, i skos ma się otwierać razem z boxem, a nie box się
otwiera a reszta zostaje."*
- **No shelf above a shoe box.** Where the stack law would cap it with a
  shelf, the shoe box is the exception: nothing sits over it. Read where the
  capping shelf is emitted (T65 F5 touched the same site) and exclude the
  shoe case there, named, with the owner's sentence in the comment.
- **The sloping shelf travels with the box.** It is part of the assembly, not
  a fixed element: one set of runners, it comes forward with the front.
- **LISP FIRST**: `KIT_WARDROBE_FULL.lsp` states both facts before the engine
  does; parens re-verified.
- Both apps get it — it is the engine.

## F2 · THE LEFT COLUMN STOPS SPECIFYING
Owner, circling them: *"jak dodajemy internal drawers, to te informacje —
tie, belt, with fronts, bare boxes — wywal proszę."*
- The INSIDE drawers row keeps **COUNT · HEIGHT · ADD**. The chips
  WITH FRONTS / BARE BOXES / INSERT / STANDARD / BELT-TIE / BELT-TIE+GLASS
  and the paragraph under them leave the column.
- They re-home in the **docked editor** for that stack (F3), named one by one
  in the PR. Left adds, right edits — no control is lost.

## F3 · THE DOCK TELLS YOU WHAT FITS INSIDE
Owner: *"dodaj do menu po prawej spec — zmianę frontu, jak by ktoś chciał; i
jak już dajesz wysokość frontu, to daj gdzieś informację, ile będzie miała
szuflada w środku boxa."*
- The stack's docked editor carries the specification moved from F2 (front or
  bare box, insert kind) plus the front-height field it already has.
- **Beside every front height, the INNER box height** — the usable clear
  height inside the drawer, derived from the engine (front height minus the
  box's own construction), never typed. One quiet line: "front 150 · inside
  NNN". If the engine does not expose it, derive it at the read site from
  values it does expose; do not invent a number and do not add an engine key
  the cut path would read.

## F4 · DOORS ON THE FIRST LINE
Owner: *"dodawanie drzwi jest tak schowane, że dopiero w accessories można
znaleźć — a powinno być na pierwszej linii, zaraz pod style: ADD DOORS /
REMOVE DOORS."*
- FRONTS gains **ADD DOORS · REMOVE DOORS** immediately under the STYLE list.
  EXTRAS keeps its entry — same store path, two doors to one law (the HANDLES
  pattern from T69 F8).

## F5 · FRONTS: SHEEN, AND MORE THAN ONE COLOUR
- **A SHEEN control sits under the front colour** (matt → satin → gloss), the
  engine's own sheen vocabulary from the profile — read it before writing the
  control; if PRO has a sheen surface, copy it.
- **A design may carry two or three front colours.** Under COLOUR: "ALL
  FRONTS" (today's behaviour) plus **+ ADD A SECOND COLOUR** / **+ A THIRD**,
  each colour applied to the fronts the client picks on the stage (click a
  front while a colour row is active). Uses the store's existing
  `setUnitFinish` / `resetUnitFinish` — the per-unit override that has sat
  unused since T60 — never a second palette law.
- The estimate line and REVIEW name every colour used.

## F6 · THE BAY STILL DOES NOT LIGHT UP
T69 F6 claims this fixed and its walk agreed; the owner says it does not
happen. **Probe first**: which element carries the hover handler, what the
chip emits, what the scene listens for — commit the table. Then fix what the
probe convicts. If the probe shows it working, say so plainly with the exact
element and event, so the owner can tell us where he is hovering.

---

## KNOWN GAPS — NOT TONIGHT, STATED SO THEY ARE NOT LOST
The price law (nothing prices a design; "Price on request" everywhere) · the
Egger mail before public launch · page content for COLLECTIONS / MATERIALS /
JOURNAL · the hero image · font approval · mirrors · GROOVED and ARCHED
machining · the L-shaped wardrobe as a furniture type. Repeat this list in
the PR body.

## TESTS AND PROOF
Full suite; goldens ×6; parens; classifier; freeze, boundary, fidelity,
rig-parity green. New: no shelf above a shoe box; the slope travels with the
box (one assembly, one runner set); the INSIDE row down to COUNT/HEIGHT/ADD
with every chip present in the dock; inner height shown beside front height
and equal to the engine's own; ADD/REMOVE DOORS in FRONTS calling the same
path as EXTRAS; sheen writing the profile's vocabulary; a second and third
front colour landing on chosen fronts via `setUnitFinish`; the bay hover
probe. Frames per F plus the lazy run.

## LICENSED REMOVALS
The specification chips and their paragraph from INSIDE's drawer row (they
move, F2). Nothing else; tombstones ≤2 lines.

## BALANCE
Per F files/lines; the probe verdict; each chip's new home; where the inner
height is derived; one-line answers — how many paths add a door (one), how
many write a front finish (one).

## SKIP-AND-NOTE ORDER
F6 → F4 → F3 → F2 → F5 → F1.
F1 is the turn's reason and is not skipped unless a golden would move.