# T68 · F4 — THE PROBE

_Generated by `node scripts/t68-f4-probe.mjs`. Nothing below is typed by hand._

The dock's own `WORKSHOP_FIELDS`, read off `detail/docked.jsx`: `setback`, `setback-unit`, `thickness`, `thickness-ep`, `carcass-board`, `front-board`, `partition-slot`, `partition-drill-face`, `runner-variant`, `material`.
Its modal names: `element`, `rail`, `watch-layout`.

## 1 · CLICK A DIVIDER — WHAT DOCKS?

### INSIDE row (addFlushPartition)

- panel: `VPART-1` · `elementKind` → `partition`
- `panel.meta.itemId`: `partition_glcuage`
- `resolveSelection` → menu `partition`, item `null`
- `dockFor` → `props:ElementProperties`
- `elementFields` offers: `position-x`, `partition-slot`, `partition-drill-face`, `setback`, `thickness`, `material`
- the dock omits: `setback`, `setback-unit`, `thickness`, `thickness-ep`, `carcass-board`, `front-board`, `partition-slot`, `partition-drill-face`, `runner-variant`, `material`
- **rows a client actually sees: `position-x`**
- the field's own commit, `setPartitionX(unitId, selection.item.id, 400)`: **THREW — TypeError: Cannot read properties of null (reading 'id')**
- the same call with the PANEL's stamped id, `meta.itemId`: **moved [891] → [400]**

### a door count of 3

- panel: `VPART-1` · `elementKind` → `partition`
- `panel.meta.itemId`: `partition_fvbjtbs`
- `resolveSelection` → menu `partition`, item `null`
- `dockFor` → `props:ElementProperties`
- `elementFields` offers: `position-x`, `partition-slot`, `partition-drill-face`, `setback`, `thickness`, `material`
- the dock omits: `setback`, `setback-unit`, `thickness`, `thickness-ep`, `carcass-board`, `front-board`, `partition-slot`, `partition-drill-face`, `runner-variant`, `material`
- **rows a client actually sees: `position-x`**
- the field's own commit, `setPartitionX(unitId, selection.item.id, 400)`: **THREW — TypeError: Cannot read properties of null (reading 'id')**
- the same call with the PANEL's stamped id, `meta.itemId`: **moved [1188, 594] → [1188, 400]**

## VERDICT

- every divider docks its position field (`position-x` survives the dock's `omit`).
- **and on 2 of 2 that field CANNOT WRITE**: THREW — TypeError: Cannot read properties of null (reading 'id') · THREW — TypeError: Cannot read properties of null (reading 'id')
- the SAME store setter DOES move the divider when it is given the id the ENGINE stamped on the panel (`meta.itemId`) — so the setter is sound and the id is the fault.

So the control is not missing — `adapter.resolveSelection` never looks a
PARTITION's item up (it does it for `shelf` and for `drawers`, and for nothing
else), so the dock is handed `item: null` and the field commits against
`null.id`. The divider cannot be moved because the one road to the setter is
broken at the selection, not at the control.

## 2 · WHERE THE OLD CONTROL WROTE

### the deleted `PartitionMenu` — 1 site(s)

```
src/retail/design/Options.jsx:408:          `PartitionMenu`. Where ONE divider stands is the docked editor's
```

### `setPartitionX` — the store's own setter — 12 site(s)

```
src/components/ElementProperties.jsx:60:  const setPartitionX = useProjectStore((s) => s.setPartitionX);
src/components/ElementProperties.jsx:384:              onCommit={(v) => setPartitionX(
src/components/RightPanel.jsx:78:  const setPartitionX = useProjectStore((s) => s.setPartitionX);
src/components/RightPanel.jsx:917:                        onCommit={(v) => setPartitionX(
src/engine/partitionPositions.js:52: * (`store.setPartitionX` and `engine/zones.js`), and a display mapping is not a
src/engine/partitionPositions.js:112: * not clamp — the clamp is `store.setPartitionX`'s, exactly as above.
src/retail/design/adapter.js:2244: * The ends are `projectStore.setPartitionX`'s own clamp, read back off the
src/retail/design/adapter.js:2271:export const setPartitionPos = (unitId, itemId, mm) => S().setPartitionX(unitId, itemId, mm);
src/retail/design/detail/ElementProperties.jsx:60:  const setPartitionX = useProjectStore((s) => s.setPartitionX);
src/retail/design/detail/ElementProperties.jsx:384:              onCommit={(v) => setPartitionX(
src/retail/design/reasons.js:166:   * T61 F4 · PREDICATE: `projectStore.setPartitionX` answering
src/stores/projectStore.js:6011:  setPartitionX: (unitId, itemId, xRaw) => {
```

### `centrePartitions` — 7 site(s)

```
src/components/RightPanel.jsx:77:  const centrePartitions = useProjectStore((s) => s.centrePartitions);
src/components/RightPanel.jsx:859:                    const moved = centrePartitions(unit.id);
src/retail/design/Options.jsx:416:            onClick={() => A.centrePartitions(unit.id)}
src/retail/design/adapter.js:995: * store's own `centrePartitions`. Retail never places a partition by hand.
src/retail/design/adapter.js:1352:  S().centrePartitions(unitId);
src/retail/design/adapter.js:2274:export const centrePartitions = (unitId) => S().centrePartitions(unitId);
src/stores/projectStore.js:5399:  centrePartitions: (unitId) => {
```

### `chainFromX` / `xFromChain` — 14 site(s)

```
src/components/ElementProperties.jsx:24:import { chainFromX, xFromChain } from '../engine/partitionPositions.js';
src/components/ElementProperties.jsx:367:        // One mapping, two cases of it. STORAGE DOES NOT MOVE: `xFromChain`
src/components/ElementProperties.jsx:380:              value={chainFromX({ x: mine, boardT: G, others: siblings })}
src/components/ElementProperties.jsx:385:                unit.id, item.id, xFromChain({ value: v, x: mine, boardT: G, others: siblings }),
src/components/RightPanel.jsx:18:import { bayWidthsOf, chainFromX, xFromChain } from '../engine/partitionPositions.js';
src/components/RightPanel.jsx:907:                          STORAGE DOES NOT MOVE: `xFromChain` puts back exactly
src/components/RightPanel.jsx:913:                        value={chainFromX({ x: mine, boardT, others })}
src/components/RightPanel.jsx:918:                          unit.id, pt.id, xFromChain({ value: v, x: mine, boardT, others }),
src/engine/partitionPositions.js:102:export function chainFromX({ x, boardT, others = [] }) {
src/engine/partitionPositions.js:114:export function xFromChain({ value, x, boardT, others = [] }) {
src/retail/design/detail/ElementProperties.jsx:24:import { chainFromX, xFromChain } from '../../../engine/partitionPositions.js';
src/retail/design/detail/ElementProperties.jsx:367:        // One mapping, two cases of it. STORAGE DOES NOT MOVE: `xFromChain`
src/retail/design/detail/ElementProperties.jsx:380:              value={chainFromX({ x: mine, boardT: G, others: siblings })}
src/retail/design/detail/ElementProperties.jsx:385:                unit.id, item.id, xFromChain({ value: v, x: mine, boardT: G, others: siblings }),
```

### `position-x` — the field in the copied editor — 4 site(s)

```
src/components/ElementProperties.jsx:362:      case 'position-x': {
src/engine/elements.js:254:  partition: ['position-x', 'partition-slot', 'partition-drill-face', 'setback', 'thickness', 'material'],
src/retail/design/adapter.js:2236: * `['position-x', 'partition-slot', 'partition-drill-face', 'setback',
src/retail/design/detail/ElementProperties.jsx:362:      case 'position-x': {
```

### the stage's own drag handles — 7 site(s)

```
src/3d/EdgeHandle.jsx:43:export default function EdgeHandle({
src/3d/UnitView.jsx:99:import EdgeHandle from './EdgeHandle.jsx';
src/3d/UnitView.jsx:2540:        <EdgeHandle
src/3d/UnitView.jsx:2568:          <EdgeHandle
src/3d/UnitView.jsx:2593:            <EdgeHandle
src/engine/profile.js:3021:      // handle's (3d/EdgeHandle.jsx): a line drawn ON a surface is a coin toss
src/retail/main-retail.jsx:83:setChromePart('edge', true);         // EdgeHandle — the drag handles on a unit's edges
```

### retail's adapter on a partition position — 2 site(s)

```
src/retail/design/adapter.js:2248:export function partitionTravel(unitId, itemId) {
src/retail/design/adapter.js:2271:export const setPartitionPos = (unitId, itemId, mm) => S().setPartitionX(unitId, itemId, mm);
```

## 3 · WHAT THE RETAIL STAGE OFFERS A HAND

```
(nothing — `Stage.jsx` carries no pointer drag at all)
```
